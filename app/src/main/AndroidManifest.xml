<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- 📌 Permissões -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" /> <!-- 📌 Hardware opcional -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Minhaparte"
        android:usesCleartextTraffic="true"
        tools:targetApi="31"
        >
        <activity
            android:name=".Activity.ListaDesempenhoActivity"
            android:exported="false" />
        <!-- 🔥 ATIVIDADES DO APP -->
        <!-- Tela principal AGORA É MainActivity -->
        <activity
            android:name=".Activity.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Login -->
        <activity
            android:name=".Activity.MainActivity"
            android:exported="true" /> <!-- Criar usuário -->
        <activity
            android:name=".Activity.CriarUsuarioActivity"
            android:exported="true" /> <!-- Feed -->
        <activity
            android:name=".Activity.FeedActivity"
            android:exported="true" /> <!-- Quiz -->
        <activity
            android:name=".Activity.QuizActivity"
            android:exported="true" /><!-- Pergunta aberta -->
        <activity
            android:name=".Activity.RespostaAbertaActivity"
            android:exported="true" /> <!-- Editor -->
        <activity
            android:name=".Activity.EditorActivity"
            android:exported="true" /> <!-- Upload -->
        <activity
            android:name=".Activity.UploadActivity"
            android:exported="true" /> <!-- Enquetes -->
        <activity
            android:name=".Activity.EnquetesActivity"
            android:exported="false" /> <!-- Player -->
        <activity
            android:name=".Activity.PlayerActivity"
            android:exported="false" /> <!-- Lista de vídeos -->
        <activity
            android:name=".Activity.VideoListActivity"
            android:exported="true" /> <!-- Lista de usuários -->
        <activity
            android:name=".Activity.ListarUsuariosActivity"
            android:exported="true" /> <!-- Perfil -->
        <activity
            android:name=".Activity.PerfilUsuarioActivity"
            android:exported="false" /> <!-- Trocar senha -->
        <activity
            android:name=".Activity.TrocarSenhaActivity"
            android:exported="true" />
        <activity
            android:name=".Activity.InfosDeUsuario"
            android:exported="true" />
    </application>

</manifest>