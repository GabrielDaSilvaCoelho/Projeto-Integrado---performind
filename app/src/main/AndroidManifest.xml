<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:label="Performind"
        android:icon="@android:drawable/ic_menu_info_details"
        android:roundIcon="@android:drawable/ic_menu_info_details"
        android:allowBackup="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApp">

        <!-- LAUNCHER: a classe que realmente existe -->
        <activity
            android:name="com.seuapp.quiz.EditorActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Outras Activities (ajuste para as suas reais) -->
        <activity
            android:name="com.seuapp.quiz.QuizActivity"
            android:exported="false" />

        <activity
            android:name="profiel.CriarUsuarioActivity"
            android:exported="false" />
    </application>
</manifest>
